# Design references

Start with [Frameleaf](frameleaf/README.md) for the approved interface direction, reusable interactive template, original SVG brand kit and agent instructions.

The older `immich-*` assets remain compatibility/history references. Use Frameleaf's supplied artwork for new identity work; do not rename or replace compatibility-sensitive application identifiers through a global asset rename.
