<script lang="ts">
  // FL-193: stands in for `AttributionControl` / `GeoJSON` / `MarkerLayer` in specs that mock
  // `svelte-maplibre`. `MapLibreStub` never renders its children snippet, so none of these ever
  // mount in practice; this only needs to exist so the mocked module has something to export.
</script>
