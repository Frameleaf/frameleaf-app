# Agent handoff, 2026-09-23 evening (cleanup of duplicated and nonconforming UI)

Continues [12-agent-handoff-2026-09-23.md](12-agent-handoff-2026-09-23.md). Read `AGENTS.md`, `CODEX-HANDOFF.md` and page 12 first; their rules still apply. This page is a checkpoint written while work continues. Always verify branch heads with `git fetch frameleaf` before acting on anything below.

## 1. Why this pass happened

The owner reported that Codex, working on the same integration branch (`claude/frameleaf-implementation`, PR #137) in the afternoon, duplicated work and did not follow the September 22 prototype. A Claude swarm audited three areas (Command Center/admin, library/viewer/people/search, server/SDK/docs), removed duplicates, fixed nonconforming screens, and dispatched the larger gaps as separate branches. Each change was reviewed by `reviewer-a` (privacy, data, auth) or `reviewer-b` (UI, duplication) before it merged. Nothing was merged to `fork/main`, published or deployed.

## 2. Binding rules added or confirmed today

- **Prototype only.** Screens are built from `design/frameleaf/template/src`. Never apply old Immich placement, never use an interim home or a dialog in place of a prototype page. Utilities are sections of the Command Center `utilities` area; Library Care is the prototype `care` screen; settings pages come from `settings-catalog.mjs`. Reviewers treat violations as P1.
- **Owner-confirmed product decisions:** FL-64 Masks and "Your presets" stay in the quick editor; FL-106 exports become Locked when a source later becomes Locked; FL-75 migration lives in Settings → Storage & originals → "Move or export your library".
- **Jsonb:** raw inserts cast `sql\`${JSON.stringify(v)}::text::jsonb\``; a unit guard fails on `::jsonb`after`JSON.stringify`.
- **Old PRs #127–#133** are recovery sources, not superseded; do not close them.
- **Local runs:** this machine may run full suites; hosted Actions remains the merge authority. When several agents run e2e, each uses its own Docker compose project and ports (specs reset the database).

## 3. Merged into the integration branch this evening

| Merge        | Branch                                                            | What                                                                                                                                                                                                                                                                                                                                                                                        |
| ------------ | ----------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ac3544519c` | `claude/frameleaf-design-rules-sep23` (PR #136, contains PR #135) | Design revision now an ancestor of #137; kept the Material icon-name check, shared-link password toggle `type="button"`, the retag publishing-gate check, extra prototype DOM assertions                                                                                                                                                                                                    |
| `e5d160c94e` | `codex/FL-83-cleanup-server`                                      | No server duplication found; restored FL-34 derivative evidence page (Docs Build broken link); regenerated FL-25 reconciliation evidence                                                                                                                                                                                                                                                    |
| `464e24762f` | `codex/FL-83-cleanup-admin`                                       | One utilities host (`/user-settings?area=utilities`, admin URL redirects); removed duplicate Library Care tool list and six orphaned components; "Your library" nav order; geolocation spec race; Studio `preview.request`/`preview.release` in the command catalogue (empty payload allowed only for listed non-mutating commands); library-admin contract 23 admin / 16 personal sections |
| `a722023ffb` | `codex/FL-82-redact-workflow-extra`                               | Workflow and step `extra` credentials redacted in every response and preserved across saves, including steps saved without `extra`                                                                                                                                                                                                                                                          |
| `4afe2ea22b` | `codex/FL-83-cleanup-library`                                     | Removed the upstream Immich command palette (Ctrl/Cmd+K and "/" open Frameleaf search only); rail/account Locked entries open `/locked`; album Map opens `/map?albumId=`; seven orphaned legacy components deleted; prototype labels; "Remove from shared link" owner-view bug; web e2e project fixed (40 pass, 1 skip locally); memory-viewer UI e2e 10/10                                 |
| `8b772cdaa9` | `codex/FL-71-command-center-shell`                                | Phase 1 of the prototype Command Center: one full-screen shell for every account                                                                                                                                                                                                                                                                                                            |

## 4. In flight (pushed branches, not yet merged)

| Branch                                            | Owner agent    | State                                                                             | Next step                                                                                                                                                                                                                                                                                                                                                                               |
| ------------------------------------------------- | -------------- | --------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `codex/FL-71-command-center-areas` @ `1e95a51bcf` | `cc-shell`     | Phase 2 built (tsc and svelte-check clean); coverage re-pin and final checks next | Overview, Sharing, Maintenance, Users, Trash, Preferences areas; Compute & jobs sections (queues, workers, cloud processing); deduplication under Storage & originals; Library care health/repair/enrichment sections; rail Settings → Overview, rail Trash → Trash area; old routes as redirects. Review `reviewer-b` (+ `reviewer-a` for Users/Trash/Maintenance privacy), then merge |
| `codex/FL-83-library-gaps` @ `f38da721b1`         | `library-gaps` | `reviewer-a` approved-with-P3; `reviewer-b` pending                               | Prototype ports of the album "Add photos" flow (select from Library with toast), the Map screen (`MapView.jsx`), and the public shared-link viewer (`PublicViewer.jsx`)                                                                                                                                                                                                                 |
| `codex/FL-83-viewer-fixes` @ `b1d2073242`         | `fix-viewer`   | `reviewer-b` pending                                                              | Viewer gaps V-1, V-2, V-5, V-14, V-19, V-21; trash-page double-delete race; zoom keys yield to the face editor; trash/face-editor/detail-panel e2e (17/17, 14/14, 7/7 locally). `check:svelte` 0 errors (use `pnpm run check:svelte`, not `--tsconfig`, which reports 6 unrelated spec errors)                                                                                          |
| `codex/FL-83-e2e-timeline` @ `59ccb156cd`         | `e2e-timeline` | Ported, review next                                                               | UI e2e `timeline.e2e-spec.ts` 47/47 locally, 0 skipped, ~37 s: 44 legacy tests ported to prototype semantics (Browse default, D/M/Y group by, G go to date, X select), 2 deleted (album add-photos picker, removed by `App.jsx:2542-2547`), 3 new; product fixes committed. REVIEW-REQUEST to `reviewer-b`, then merge                                                                  |

Merge order: `library-gaps` and `viewer-fixes` in either order, `e2e-timeline` after `cleanup-library` (done) and ideally after `viewer-fixes` (shared labels), `FL-71` phase 2 whenever reviewed. Each branch re-merges the head, re-pins the documentation-coverage `sourceAnchorSha256` from the committed tree, re-checks and pushes the merge itself (procedure in the session's cleanup brief, reproduced here: fetch, `git merge --no-ff` the head, run checks, `git push frameleaf HEAD:claude/frameleaf-implementation`, retry up to three times, never force).

## 5. Held, not merging

- `codex/FL-83-conformance-audit` @ `3e99b00384`: unreviewed audit branch, ~200 commits behind, conflicts with Codex's in-flight auth/PIN work. Its audit document and conformance evidence remain useful references; its code changes were superseded by the cleanup branches.

## 6. Open owner questions

- **FL-34 Locked:** the prototype's Locked view combines locked items and rule-hidden items; production splits them across `/locked` and `/suppressed` (the latter is now unlinked but still PIN-gated). Joining them needs server work and privacy review. Recorded as FL-34 follow-up; owner to choose who takes it (Codex has unpushed FL-83 browser-privacy recovery nearby).
- **FL-60 rule matches in the info panel** (`DetailPanelClassification`): required by FL-60 acceptance but absent from the prototype's info panel. Kept as built pending the owner.
- **FL-78 `DELETE /libraries/{id}`:** stable public endpoint that bypasses FL-78's two-stage review. Recommendation: keep it for official clients but apply the same permission recheck and consequence handling; alternatives are requiring the review token or retiring it.
- **Codex coordination:** Codex has unpushed work in `~/.codex/worktrees` for FL-71 (now taken over by `cc-shell`), FL-80 auth, FL-32 prepared archive, FL-39 video history, FL-29 prototype foundation and FL-83 browser privacy. Before pushing, Codex must merge the current head; expect conflicts in `web/src/routes/+layout.svelte` (command palette removed), `e2e/src/specs/web/auth.e2e-spec.ts`, Locked navigation (`navigation.ts`, `AccountMenu.svelte`, `TopBar.svelte`), and the settings host (`SettingsHost.svelte`, `settings-areas.ts`, `routes/admin/system-settings/+page.ts`). FL-71 should not be built twice.
- Earlier open questions on page 12 §5 still stand (FL-74 limits and Confluence mirror of `preservation.md`, FL-66 P3s, FL-79 host-files row, FL-82 All-accounts filter, FL-75 intro wording).

## 7. Other follow-ups

- FL-82 P3: also scrub run-log error messages against step `extra` (`workflow-execution.service.ts` `redactRunError`); no current path exposes it.
- `Rail.svelte` / `TreeBranch.svelte` are unused but documented in the Confluence-mirrored `FL-29-web-primitives.md`; delete both together with a mirror update.
- Confluence sync remains blocked in these sessions (Atlassian connector unauthorized); `docs/docs/features/preservation.md` still needs its mirror refreshed.
- Hosted CI was 31 passing, 3 pending, 3 skipped on the head at the time of writing; confirm on the final head.
