# FL-30 production navigation rollout

This is a bounded web implementation of [FL-30](https://heroit.atlassian.net/browse/FL-30), based on accepted `fork/main` commit `fc9d12d38ab707dd368298df84af2848106529be`. The [implementation plan](https://heroit.atlassian.net/wiki/spaces/FR/pages/61538319) and action-preservation ledger remain authoritative. FL-30 stays In Progress; native applications are deferred.

## Implemented behavior

Enable **Frameleaf navigation** in User settings → App settings. This browser-local `frameleaf-shell` preference defaults to false. Disable it to restore the existing navigation immediately. The preference selects navigation markup and scoped appearance inside the existing Sidebar; it does not wrap/remount route content, clear drafts, navigate, change a server capability, or rewrite library data. Existing theme, `sidebar-collapsed`, sidebar width, album tree and recent-album preferences remain authoritative.

`web/src/lib/components/frameleaf/route-registry.json` supplies the real navigation entries consumed by `Navigation.svelte` through the existing `Route` helpers. Timeline comes first. Explore, Best Photos and Locked retain their established destinations. Album hierarchy/recent albums retain their existing components and data services. Recently added appears once and respects its visibility preference. Utilities and Trash sit in the Settings navigation group; there are no duplicate-review/large-file/Live Photo shortcuts in the Library group.

The production Sidebar retains responsive overlay behavior, focus trapping, Escape/outside dismissal and collapse control. Collapsed link text is visually hidden while remaining available to screen readers. Frameleaf tokens are scoped to the opt-in sidebar and use the existing theme manager. `Brand` uses the unchanged supplied `frameleaf-logo-white.svg` on its deliberate dark background; original artwork is unchanged. The initial navigation packet retained the existing header and route content. The Settings overview extension below moves storage and server status out of the opted-in library rail; the legacy rail is retained for rollback.

## Preservation and authority

The registry is presentation metadata, never an authorization decision. Search/Map/Trash still follow server capabilities. People, Memories, Shared links, Tags and Folders require their existing enabled and sidebar-visible preferences. Backend checks and route loaders remain unchanged, including PIN-sensitive Locked behavior, administrator authentication, public share key/password handling, maintenance authentication and login continuation. The accepted 70-route inventory remains the complete preservation catalog; this smaller navigation registry does not claim to replace it.

No production Studio, Activity, Browse/Work session or command-center route is fabricated from dirty-only evidence. Public/auth/error/admin shells keep their established loaders/layouts. The historical Confluence web-shell snapshot (page 61440119) describes unreviewed application work; its build-time flag and absent routes are not implementation evidence for this packet.

## Verification and remaining acceptance

Focused unit/component tests exercise real route resolution, capability and separate preference gating, unique destinations, deep-route active links, expanded/icon navigation, persisted collapse and in-place appearance rollback. The route-inventory tests retain all accepted families. These tests do not establish browser-back restoration, server authorization, actual screen-reader output, visual fidelity across viewports or draft recovery in an authenticated application.

Run from `web/` with the pinned Node 24 toolchain:

```sh
pnpm exec vitest run src/lib/components/frameleaf/navigation-registry.spec.ts src/lib/components/frameleaf/navigation-component.spec.ts src/lib/components/sidebar/Sidebar.spec.ts --maxWorkers=2
```

Run `node --test scripts/frameleaf-route-inventory.test.mjs` and `node scripts/frameleaf-documentation-coverage.mjs` from the committed candidate root. Independent permission/privacy review and hosted checks are separate delivery gates. Full Settings utilities/Trash workflow redesign, command-center system information, shared session/Browse/Work, Studio/Activity, collection slideshow qualification, auth/public/error branding, authenticated deep-link/back/draft checks and wider accessibility/visual acceptance remain open. No API, schema, migration, native or worker changes are included.

## Authenticated browser follow-up — September 21, 2026

Local Chromium qualification used combined candidate `deee52d075b8623486c8ddf376b48483692b2546` with this reviewed navigation source, a freshly built current server/plugin SDK, and fresh disposable PostgreSQL 14 VectorChord/Valkey 9 instances. It did not reuse the earlier backend build or database. The existing fixture created two synthetic accounts and two generated PNGs through real signup/login/upload/PIN APIs, then seeded face/derivative/EXIF associations and active-fork configuration. Authentication, HTTP responses and WebSockets were not intercepted. This setup does not qualify background media/face processing or database cutover.

The bounded browser pass verified:

- The actual Frameleaf navigation switch toggles on/off/on without losing an unsaved profile-name draft. A real `getMyUser` read confirmed the backend name was unchanged; Save was never invoked. Rollout preference and collapsed state survive reload.
- Timeline and Best Photos navigate to their production routes; browser Back restores Photos with the correct active link. Direct `/utilities/large-files` navigation activates Utilities.
- Collapsed links retain names in Chromium's accessibility tree and Favorites works with keyboard Enter. At 820 and 390 pixels, overlay keyboard focus remains inside the sidebar; Escape closes it and restores Main menu focus.
- Real People/Recently added sidebar preferences hide their links, and Recently added is unique when visible. Disabling Map/Trash in the disposable server configuration hides those destinations. The original preferences/configuration were restored afterward.
- A real public share renders its synthetic image without the private rail, even with the browser rollout preference enabled. Guest Photos redirects to login and an unauthenticated asset request returns 401. A regular user is redirected from administration to Photos, receives 403 from the admin API, and receives 400 for the other owner's asset. The synthetic share was removed after checking.
- Settled dark/light sidebar colors match the scoped tokens: dark `rgb(23,29,33)` and light `rgb(255,255,255)`, with green active text and no horizontal document overflow. Inspected desktop and phone captures show readable labels, separators, focus treatment and the supplied wordmark on its dark plate. An initial light screenshot caught the 200 ms theme transition and is superseded by the settled capture. All browser passes reported zero page errors.

The durable local bundle is `/Users/adamtaylor/.codex/worktrees/frameleaf-fl30-browser-evidence/`. Its `README.md` records reproduction and limits; `navigation-proof.json`, `access-proof.json` and `visual-proof.json` record assertions; matching `.mjs` drivers, accessibility trees and PNG captures retain runnable evidence. `desktop-light-settled.png` is the authoritative light capture. Disposable session state was excluded from the bundle and deleted after all owned test processes/containers were stopped. No tracked runtime source changed during qualification.

This closes only the named local draft/navigation/access checks above. The existing header identity and main layout remain unchanged; the captures do not establish full prototype fidelity. Actual screen-reader software, other browsers, every route/deep-link family, maintenance recovery, all PIN/privacy cases, media/worker quality, collection slideshow, complete Settings utilities/Trash redesign, shared session/Browse/Work, Studio/Activity and auth/public/error branding remain open. Hosted checks, merge/publication and deployed behavior remain separate evidence. Native work is deferred and FL-30 remains In Progress.

## Settings overview extension — September 21, 2026

The same default-off `frameleaf-shell` preference now enables `SettingsOverview.svelte` on the existing authenticated `/user-settings` route. It adds real server filesystem capacity from `getStorage`, installed version/build from `getAboutInfo`, current-session account quota/usage from `authManager`, and the existing WebSocket connection status. Personal logical usage is explicitly separate from whole-filesystem usage, which includes other accounts and non-library files. Null account usage is Unknown, and unlimited quota is distinct from zero capacity. These figures do not infer library health or recovery readiness.

Server reads have loading, partial-failure and retry states; a storage failure does not hide a successful version response or manufacture zero values. Refresh obtains new server snapshots. About fetches real version history and opens the existing About modal, preserving dependency attribution; it has a retryable failure state and cannot open after the overview unmounts. No update polling, release-manager requests, administrative APIs, storage mutations or backend/schema changes were added.

Settings embeds the existing `UtilitiesMenu` with all existing routes and download-modal actions, including the existing administrator-only missing/corrupt media entries. The duplicate, large-file and Live Photo tools therefore remain under Settings → Utilities without adding library rail shortcuts. Trash uses the existing route and remains gated by the server Trash capability. This is an integrated entry point, not qualification of the tools' destructive actions or a complete Trash workflow redesign.

The overview uses the approved scoped Theme/tokens and responsive one/three-column metrics. In the opted-in rail, the old storage/server footer is removed; its About and connection status are available on Settings, while the existing purchase/support action remains in the rail. Disabling the preference restores the full legacy footer. `UserSettingsList` remains a stable sibling outside the conditional overview, so its forms, accordion/query handling, OAuth callback and unsaved drafts are not intentionally remounted by rollout changes.

Seven additional component tests exercise distinct metric scopes, null/unlimited account values, independent server failure/retry, actual utility destinations and administrator visibility, Trash capability, About history failure/retry and unmount cancellation. Together with the original navigation and sidebar tests, 31 focused tests pass. Svelte and TypeScript diagnostics, focused lint/format, route-inventory and committed-source documentation checks are recorded in the owner handoff. The earlier `deee52d` authenticated browser evidence applies only to the preceding navigation packet; this overview still needs independent review and fresh authenticated/browser/visual qualification.

Full command-center processing, actionable issues and database/recovery readiness remain unimplemented in this packet. Full Settings/Utilities/Trash workflow design, collection slideshow, shared session/Browse/Work, Studio/Activity, public/auth/error branding, all route/access cases and actual screen-reader/cross-browser acceptance remain open. Native work is deferred. FL-30 remains In Progress; no push, hosted checks, merge, publication or deployment is claimed.

## Overview browser findings and keyboard/header fixes — September 21, 2026

Independent review approved overview candidate `d86e70990e485bffdc6fe6c14436ba6874967110`. Its fresh authenticated browser receipt is `/Users/adamtaylor/.codex/worktrees/frameleaf-fl30-overview-evidence/README.md`, with runnable drivers and JSON assertions. Eleven bounded checks passed: real account/filesystem/build values, loading and explicitly injected transport-failure recovery, actual About/history, purchase preference visibility, utility route/Back behavior, server Trash capability, unsaved draft rollout retention, responsive overview cards, and real regular-user storage/about 200 versus administrator API 403. No page errors occurred. Synthetic accounts/media and a fresh disposable current-backend database/cache were used; no live/personal library was touched.

That browser pass also exposed an unnamed shared About dialog, missing overview focus return, and inherited header overflow (420 px document width at a 390 px viewport with rollout both on and off). These are failures, not accepted visual/accessibility results. The shared `@immich/ui` dialog naming correction is separately root-owned under FL-29 and is not included in this owner branch.

Commit `79b474fbecd592641d7791464e85a1b69efb8225` captures the actual About trigger, awaits the asynchronous modal close, reenables it and restores focus after a Svelte tick only if the overview remains mounted and the trigger connected. Narrow header spacing now uses the available width; all controls remain mounted, with additional controls contained in a horizontally scrollable action row rather than widening the document. Existing header height, route services, notification/account panels and action handlers are preserved.

The first focus adjustment exposed another concrete keyboard issue: initial focus on the Close button activates its tooltip and the first Escape dismisses that layer. Commit `2f5b0eb88cdfed9e1c51843f20e148b55a271ba9` instead initially focuses the About dialog container and retains native modal Escape behavior. The focus-return regression fails on d86 source; the real About-component initial-focus regression fails on 79b source; both pass with the final source. Unmount cancellation remains covered.

The separate targeted fix bundle is `/Users/adamtaylor/.codex/worktrees/frameleaf-fl30-overview-fixed-evidence/README.md`. `header-proof.json` at 79b verifies no document overflow at 320/390/820/1440 with the extra manual-theme control present, keyboard-operated account/notification panels without clipping, and keyboard reachability of scrolled actions. `focus-regression-proof.json` at 2f verifies initial dialog focus, single-Escape dismissal and trigger restoration at all four widths, plus pointer Close restoration, with zero page errors. Backend/SDK were freshly rebuilt at 79b and their source is unchanged at 2f. The partial broad 79b pass is retained as a failed intermediate receipt; it is not represented as a complete pass. Owned services/containers were stopped and disposable session state deleted.

These fixes still require independent exact-candidate review. Final combined-source browser qualification with the separate FL-29 accessible-name patch remains root-owned. The broad command-center, workflow, branding, authorization-family and screen-reader/cross-browser acceptance listed above remains open; native applications remain deferred. No hosted CI, push, merge, publication or deployment is claimed by this receipt.
