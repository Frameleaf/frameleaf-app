# Agent handoff, 2026-09-24 (overnight integration pass)

Continues [13-agent-handoff-2026-09-23-evening.md](13-agent-handoff-2026-09-23-evening.md). Read `AGENTS.md`, `CODEX-HANDOFF.md`, page 12 and page 13 first; their rules still apply. Verify every SHA below with `git fetch frameleaf` before acting on it.

## 0. For a local agent on another computer

This page lives on GitHub in `Frameleaf/frameleaf-app` on branch `claude/frameleaf-implementation` (PR #137) at `docs/docs/developer/frameleaf-plan/14-agent-handoff-2026-09-24.md`. The cloud session that wrote it may be gone; everything you need is in the repository.

1. Get the repository and name its remote `frameleaf` (AGENTS.md forbids pushing to a remote named `origin`). With an existing clone: `git remote add frameleaf https://github.com/Frameleaf/frameleaf-app` if it is missing, then `git fetch frameleaf`. Without one: `git clone -o frameleaf https://github.com/Frameleaf/frameleaf-app`. Check `git remote -v` names `Frameleaf/frameleaf-app` and nothing upstream.
2. Read without switching branches: `git show frameleaf/claude/frameleaf-implementation:docs/docs/developer/frameleaf-plan/14-agent-handoff-2026-09-24.md`. Or check out the branch: `git switch -c claude/frameleaf-implementation --track frameleaf/claude/frameleaf-implementation` (use `git switch claude/frameleaf-implementation && git pull --ff-only` if it already exists). Never reset or clean a working tree that has someone else's uncommitted work; use a separate worktree instead (`git worktree add ../fl-integration frameleaf/claude/frameleaf-implementation`).
3. Read in this order: `AGENTS.md`, `CODEX-HANDOFF.md`, `docs/docs/developer/frameleaf-plan/12-agent-handoff-2026-09-23.md`, `13-agent-handoff-2026-09-23-evening.md`, then this page.
4. Fetch the in-flight branches in §3 (`git fetch frameleaf codex/FL-71-command-center-areas codex/FL-83-timeline-grouping`) and compare their heads with the SHAs below. If they moved, someone continued the work; read their new commit messages before touching them.
5. Commit as author and committer `AJ Taylor <aj@ajtaylor.net>` (`git config user.name "AJ Taylor"` and `git config user.email aj@ajtaylor.net` in the repository), put the Jira key in the subject and one `FL-n #comment` line per issue in the body, add no trailers, push only to `frameleaf`, never force-push, and never merge to `fork/main`, publish or deploy without the owner's say-so. Do not touch `mobile/`.
6. On a small machine (the owner's Mac has 7 GB) do not run builds or full suites; push and let GitHub Actions run them (AGENTS.md "Review and CI").

## 1. State at the time of writing

- **Integration branch:** `claude/frameleaf-implementation` (PR #137, draft, base `fork/main`). This page was last updated together with the timeline-grouping merge and the medium-test fix (§2); check `git log` for the exact head. The session branch `claude/frameleaf-implementation-faumg1` is kept identical.
- **Hosted CI on `9e70eb589f`:** 34 passed, 3 skipped, 1 failed: **Medium Tests (Server)**, `server/test/medium/specs/fork-schema/return-reconciliation.spec.ts:1029` timed out at 15 s. Root cause found and fixed in the test (§2, `fix: FL-41`); the head after this page's last update must be confirmed green on hosted CI.
- **Last fully green head:** `e2c336a7c9` (35 passed, 3 skipped, 0 failed), the first green run since the evening checkpoint.
- Nothing was merged to `fork/main`, published or deployed. `codex/FL-83-conformance-audit` stays held.

## 2. Landed on the integration branch in this pass

| Commit               | What                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| -------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `30eee6e8d8`         | Docs Build fix: page 13 used escaped backticks around a JSON example, so MDX evaluated it as JSX, and it contained literal backslash-n sequences                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `90fecd4df7`         | Maintenance e2e specs type RESTORE: FL-81's `MaintenanceRestoreConfirmDialog` ports the prototype `RestoreDialog` (`Maintenance.jsx:704-753`), whose button stays disabled until RESTORE is typed; the specs predated it                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `0214a5f41a`         | Results/Timeline deletion specs confirm the viewer's always-on permanent-delete dialog (the FL-83 viewer merge made it non-suppressible, as the prototype does); the concurrency guarantees are unchanged and still asserted                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `e2c336a7c9`         | Merge of `codex/FL-83-e2e-timeline` plus `d08048e93c` (Prettier; a day checkbox no longer moves the shift-click anchor). The "select day, then shift-click" test was not dropped: it had been renamed, and its body never selected a day                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `6370315569`         | Merge of `codex/FL-83-library-gaps` at `335cd7957f` after an independent privacy review (approve) and a UI review (approve with P3s). Public share viewer: name-only "Shared by", no library toolbar or Work panel, a slideshow entry for download-enabled links, password and unavailable states inside the public shell. `/map?albumId=` gets the settings sheet and "Search this area"                                                                                                                                                                                                                                                                                                          |
| `cc44252913`         | FL-83 note `FL-83-public-viewer-and-album-map.md` records what the owner field exposes, and when                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `e14cbf6ca6`         | **Owner decision:** the album map's "Partner items" follows the prototype literally (`filterMapAssets` treats every item someone else owns as a partner item), so off keeps only the viewer's own album items; "Shared spaces" has no effect in album scope (the prototype never sets `sharedSpaceOnly`). The filter is a single owner equality that can only narrow; reviewed by reviewer-a (approve)                                                                                                                                                                                                                                                                                             |
| `6eb7cecdde`         | Comment follow-up for the above                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `9e70eb589f`         | Regression fix: `SharedLinkService.addAssets` spreads the link loaded by `get` into `update()`, and that link now carries the read-only `owner` name, so the update tried to write a `shared_link.owner` column. `update()` now omits `owner`. Hosted Server and CLI e2e confirmed the fix on both architectures                                                                                                                                                                                                                                                                                                                                                                                   |
| (after `9e70eb589f`) | Handoff page 14 and the `CODEX-HANDOFF.md` pointer; coverage digest re-pins                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| (merge)              | Merge of `codex/FL-83-timeline-grouping` at `dc80eae898` after three review rounds (final: approve with P3): prototype month/year/all grouping, grouping control with Ctrl/Cmd-wheel, pinch and M/Y/D, full-date day headers, group checkboxes that load missing months sequentially with per-group requests and a revision guard, shift-click anchor parity with `App.jsx:570-587`. `LibraryView.svelte` conflict resolved so the public view's forced browse layout also disables grouping                                                                                                                                                                                                       |
| `fix: FL-83`         | Review P3-e: a group checkbox's focus claim ends when the group scrolls away entirely                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `fix: FL-41`         | Medium-test timeout root cause (test-only): the test destroyed its second Kysely pool while a transaction was still in flight, and postgres.js `end()` then never resolves, so any failure became a silent 15 s timeout; the trigger was `waitForBackendBlock` pairing a stale `pg_stat_activity` wait event with a live blocker. Fix: settle both transactions before `destroy()`, and poll until `wait_event_type = 'Lock'`. Reproduced locally (Postgres from Docker Hub `tensorchord/vchord-postgres:pg14-v0.4.3`); 20/20 file runs under heavy load after the fix. `waitForWriter` (line ~46) has the same stale-sample pattern but settles before destroy; hardening it is a small follow-up |

Server API changes now on the branch (review before any `fork/main` merge): `SharedLinkResponseDto.owner` (optional, the owner's display name only, loaded only by `SharedLinkRepository.get`, never before a password unlock), and optional narrow-only query filters on `GET /albums/{id}/map-markers`, which shared-link requests ignore. OpenAPI and the TypeScript SDK are regenerated. The Dart client under `mobile/` was not touched.

## 3. Branches pushed as checkpoints (not merged)

Each was fast-forwarded to the agent's latest committed head. All commits are authored and committed by AJ Taylor, with no trailers. Agents were still working when this page was written: their uncommitted edits live only in the session container and are lost if it is reclaimed.

| Branch @ SHA                                      | State                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Next                                                                                                                                                                                                                                                                                                                                                                                                 |
| ------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `codex/FL-71-command-center-areas` @ `34c15f5ca0` | Round 1 (reviewer-b changes-required: two P1s, four P2s) fixed by `10b93af77e`: redirects keep their section (`/admin/maintenance?isOpen=backups` goes to the backups section; `#workers` is read client-side), e2e specs moved to the new addresses, admin-config and trash mocks for the UI e2e, and a real `JobsManager` port replacing Immich's QueuePanel. The trash double-delete race fix is ported into `TrashSection.svelte` with `trash-neighbours.spec.ts`. The Users detail panel, "Open your trash", Overview links and the preservation ledger targets are fixed. Round 2 re-review of `10b93af77e`: no P0/P1, five P2s. Since then it has merged `e2c336a7c9` and fixed the timeline spec's Trash return (`34c15f5ca0`). The round-2 fixes were in progress, uncommitted: palette queue actions through the review, the prototype Concurrency and "Create a maintenance job" dialogs, the prototype Users detail header, the per-device queue action history, and exposing job `failedReason` and `attemptsMade` in the queue job DTO (a server change) | Finish and commit round 2 (the P2 list is in the round-2 review: `getQueueActions` bypassing review, `ConcurrencyDialog`, `ManualDialog`, the `UserDetail` header, queue action history; P3s 1-11). Run the UI e2e for Job manager and the queue redirects. Get reviewer-b to re-review, and reviewer-a for the queue DTO change. Merge the current head, re-pin the coverage digest, merge and push |

`codex/FL-83-library-gaps` (`335cd7957f`), `codex/FL-83-e2e-timeline` (`d08048e93c`) and `codex/FL-83-timeline-grouping` (`dc80eae898`) are fully merged. Open a Jira follow-up for the grouping deviation recorded in page 13 §6 (Years and All lay out per month; the prototype flows the whole group).

## 4. Decisions made in this pass

- **Album map "Partner items":** literal prototype mapping (owner delegated the call). Recorded in `FL-83-public-viewer-and-album-map.md`.
- **Public "Shared by":** display name only, never before a password unlock, omitted for deleted owners. The password prompt stays owner-neutral, a deliberate privacy deviation from the prototype's "Enter the password from …".
- **Maintenance restore:** the typed RESTORE confirmation is correct per the prototype; tests follow it.
- **Viewer permanent delete:** always confirms, as the prototype does; tests follow it.
- **Commit identity:** the session's stop hook asks for Claude-identity commits; AGENTS.md and the owner's brief require AJ Taylor, which wins. Expect that hook warning.

## 5. Open items and follow-ups

- **CI:** confirm hosted CI is green on the current head (the medium-test fix is test-only and was verified locally against Postgres).
- **Library gaps P3s (not done):** the public unavailable state should always show the prototype body text and the `mdiLinkOff` icon (it currently shows the raw API message). The password state needs the `mdiLockOutline` icon and an inline wrong-password error with `aria-invalid` instead of a toast. A distinct "link expired" state would need the server to tell expired links from invalid ones: product call.
- **Maintenance restore dialog:** it omits the prototype's "Create a safety backup of the current database first" checkbox and the consequence list (`Maintenance.jsx` `RestoreDialog`).
- **FL-71 omissions justified by server data:** the job Account and Worker columns and "Retry failed" (no retry command exists). If "Retry failed" is wanted, it needs a server queue command.
- **Jira/Confluence:** the Atlassian connector remained unauthorized. Smart Commit lines are in every commit but were not verified in Jira; no issue transitions were made. Mirror receipts are unchanged; `docs/docs/features/preservation.md` still needs its mirror refresh (page 13 §7). This page and pages 12 and 13 are not mirrored.
- Earlier open questions on page 13 §6 still stand (FL-34 Locked join, FL-60 info panel rule matches, FL-78 `DELETE /libraries/{id}`, account usage placement, FL-51 offline map base).

## 6. Working notes for the next agent

- **Coverage digest:** compute `sourceAnchorSha256` from the committed tree by importing `validateDocumentationCoverage` from `scripts/frameleaf-documentation-coverage.mjs` with the repository root and printing `.sourceAnchorSha256`; it reads `git ls-tree HEAD`, so commit first. It hashes tracked paths under backlog source paths, so adding or removing a file under, for example, `web/src/lib/components/frameleaf/` changes it; content edits do not.
- **Server unit tests:** use `pnpm exec vitest --config test/vitest.config.mjs` from `server/`, and build `packages/plugin-sdk` first (`pnpm build`) or suites fail to resolve `@immich/plugin-sdk`.
- **UI (mocked-network) e2e runs locally:** start `vite dev` for `web/` on a free port, and use a temporary Playwright config that launches `/opt/pw-browsers/chromium-1194/chrome-linux/chrome` (the pinned Playwright expects a newer build) and points at that port. The setup check needs an `e2e/test-assets/albums` directory. Use one worker on this 4-CPU machine. Delete the temporary files afterwards. Docker-backed web and server e2e and medium tests cannot run here: ghcr blob downloads return 403.
- **Merges:** use `git merge-tree --write-tree` to preview conflicts; take the integration side for `scripts/frameleaf-documentation-coverage.test.mjs` and re-pin after committing the merge.
