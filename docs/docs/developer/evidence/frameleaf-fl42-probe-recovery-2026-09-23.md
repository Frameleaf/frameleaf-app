# FL-42 endpoint probe recovery, September 23, 2026

The preserved `aj/FL-42-endpoint-capabilities` worktree was based on `0a042d249039a1eb706ed7ede96348707569c4d5`. Its proposed diagnostics API predates the current ML destination registry. This recovery starts at `8af009aad58347e1ac27baf56b4692cc00d84687` and carries the applicable transport and validation safeguards into the existing `MachineLearningRepository.probe` and `getHardware` paths. The original worktree and its unpublished files remain untouched.

Probes now distinguish credentials at the same URL, refuse in-flight observations after configuration or published RunPod endpoint changes, reject redirects, cap each diagnostic request at five seconds and decoded JSON at 32 KiB, and validate hardware against the existing response schema with bounded fields. Unsupported fields are stripped. Hardware remains informational; malformed or missing hardware does not qualify rendering or establish GPU capacity. Legacy workers with no capabilities route retain their existing library-only workload behavior. This packet adds no endpoint, migration, destination selection, cloud startup, media upload or UI.

Four targeted regressions failed on the baseline: credential cache isolation, stale configuration completion, oversized capabilities and unvalidated hardware. After the repair, 204 tests across the repository, destination service/admission helpers and configuration service passed. A disposable loopback HTTP fixture checks actual redirect refusal and timeout of a stalled body. Server TypeScript and scoped lint/format are checked separately. All remote responses in these checks are mocked or served by the owned loopback fixture; configured user endpoints and cloud providers were not contacted.

GitNexus was unavailable during this packet. Manual caller tracing identified the admin destination probe/sweep, request admission and the hardware settings route. Because admission consumes probe results, the parent identified this as higher risk and requires independent review before integration.

FL-42 remains In Progress. This is transport and observation integrity evidence, not authenticated worker identity, qualified render proof, model/memory/color admission, capacity reservation, hardware qualification, native parity or complete FN-302 acceptance. Existing restoration and Studio admission remain separate authorities. The older proposed API is not treated as delivered.
