# Frameleaf recovery checkpoint — 2026-09-23

This is execution evidence, not a specification or a claim of release readiness. The September 22 prototype remains the UI authority. The original [agent handoff](../frameleaf-plan/12-agent-handoff-2026-09-23.md) remains historical; this checkpoint records the subsequent recovery. No default-branch merge, container publication, deployment, or complete prototype acceptance is claimed.

## Reconciled sources

The takeover inspected 80 local worktree entries and 45 remote branches, including branches with no PR. Dirty worktrees were preserved. Recovery started from `frameleaf/claude/frameleaf-implementation` at `2fb1c3ae5e973d30dd12e83e643a54ec49099e25`; the freshly observed default `fork/main` was `1303c1e702f1dcaf7be3b8e7d4e38a4240c99498`. The existing integration PR is [#137](https://github.com/Frameleaf/frameleaf-app/pull/137), still draft at this checkpoint.

Only the `frameleaf` remote is a publication target. The local integration branch is `aj/frameleaf-takeover-20260923`. The following candidates were independently reviewed, repaired where needed, and integrated locally:

| Work                             | Accepted candidate                         | Important repairs                                                                                                                  |
| -------------------------------- | ------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------- |
| FL-75 migration operator flow    | `5bb33e13bb734156f6ad306a54beafc1e13a596d` | Prototype storage placement, operator wording, migration guide and mirror receipt                                                  |
| FL-106 project derivatives       | `6b0579bec064903b0283b6b74cc376ebb08722ed` | Claim and publication fencing, Locked inheritance, descendant traversal and stale publication races                                |
| FL-41 server recovery            | `cacb1da589b23e1bc3e4ba468dc1b8890b494769` | Preserve manual sensitive-content review when new detection results arrive                                                         |
| FL-41 web recovery               | `ecf59f386dbb55fe3ababca3dcab1396f881429d` | Quick-editor copy/paste and draft rebasing, Studio cleanup, person action callbacks                                                |
| FL-79 scoped analytics           | `e0537ae0d319cc9f2a68e43ad37c2be6505d3e77` | Locked counts, failed-report clearing and CSV formula neutralization                                                               |
| FL-60 classification rules       | `62f8719d0679c3ba46f4a3b86e1cce9173592402` | Owner-only manual decisions, Locked counts, archive provenance, transaction locks and current-rule fences                          |
| FL-78 external libraries         | `c14c95596a689a1536f3ec3340c32381eef21ff0` | Scan claim/lease and lifecycle fencing, empty-root protection, Locked statistics and raw path validation                           |
| FL-82 workflows and applications | `31422494769ce14b90bbec0725e8de5b662829ae` | Credential-path validation, stable step identities, retry definition fencing, dirty-close guards and retryable application loading |

Additional integration repairs include the initial library router-ready guard and removal of inactive pagination (`7ac32b88ed`), reconciled catalog and migration unions, verified documentation receipts, and core-plugin test dependency wiring (`9d6bca3730`). The catalog contains 170 tables: 137 public and 33 fork-owned.

## Verified local evidence

The combined application source through `230dafff30` passed 244 server unit files / 5,663 tests and 222 web unit files / 2,438 tests, with two web tests skipped. The web run printed local fetch/abort warnings but exited successfully. Core-plugin WASM was then built with the repository-pinned Extism 1.7.0 and Binaryen 124 toolchain. All 27 core-plugin PostgreSQL checks passed after the test fixture was updated at `9d6bca3730` to supply the real classification repository and explicit retry queue mock. Earlier combined workflow repository/service/write checks passed 19 tests.

Other completed checks:

- Server and SDK builds, regenerated OpenAPI and TypeScript client with no generated differences, and web TypeScript checks after generating SvelteKit route types.
- 127 PostgreSQL checks across classification, albums, external libraries and migration ledgers. Three initial library failures were missing pinned test assets; all passed after initializing `e2e/test-assets` at `6742055402de1aa48f93d12ded7d18f4057f9d1f`.
- The browser regression for initial library view persistence passed against a disposable local server.
- All ten documentation-coverage mutation checks passed. The current inventory is 517 source anchors, 73 Confluence receipts (37 current, 36 historical), 1,199 requirements and 1,219 ledger rows.
- Reproducibility documentation was mirrored to Confluence page `61440795`, version 7, and fully read back. Its source SHA-256 is `61c790669a0788ccbb497d1f7fdd263d9f59277e585eda2085609aec836d69ef`.

These are local results, not current-head GitHub Actions results. The complete PostgreSQL suite at `9d6bca3730` passed all 1,483 tests across 121 files, and server type checking passed. The initial fresh catalog comparison preceded normal geodata import and had two expected geodata-index shape differences; it is not full catalog certification. Smart Commit ingestion for the pushed FL-75 update remained unverified after bounded checks; no Jira completion is claimed.

## Active work and remaining acceptance

FL-83 privacy and conformance work remains outside the integration branch. Recheck of `174c811074431156780b77ac2544e038b16a400a` accepted clickable Retry through native dialogs and closure of date/location portals before unhide, but found other body-mounted confirmations and face-preview dialogs could still reveal Locked metadata. The owner is replacing incomplete portal coverage with shared lifecycle cleanup. Offscreen selection and per-asset memory operation results remain accepted.

FL-69 candidate `0b723a1768cb4f5bab694fa0213de272574394cd` was independently approved and integrated at `b6ec8a00a1`. Utilities now use the prototype Command Center directory and tool sections, with account-appropriate access and legacy URL redirects. Repairs cover accordion teardown navigation, geolocation actions respecting the visible selection, and verified Live Photo outcomes retained for the loaded review. Outcome history is not persisted across reloads. The integration passed 22 focused utility tests, 17 route/ledger checks, TypeScript and Svelte checks with zero errors or warnings. Four moved backlog anchors were updated at `6b09791780`; all ten documentation-coverage checks pass with source digest `079e756405c87945bf1809377799cb764973e1559de5803cb05125afa2267fd3`.

FL-80 is porting login, first-admin registration, forced password change and PIN screens from the prototype. The real session-cookie/OAuth contract was independently approved at `ea0f67d624f119050884b24807ca550cfc422eca` and integrated at `66e9246bfa`. An explicit false value omits persistence only on authentication cookies; omitted or true retains the existing lifetime. All 135 focused authentication and cookie-controller checks passed. Web candidate `ed46f139fe56422659d7a9b8810d6c4e2f5b171f` passed 25 focused checks, types, lint and production build. Review accepted OAuth continuation and the prototype layout, but found cancellation of a pending PIN unlock could leave the server session elevated. The owner is repairing server-state reconciliation before integration. Actual local HTTP login checks confirm false emits three session cookies, while true and omission retain the existing authentication-cookie lifetime; no cookie values are recorded in the evidence. Full onboarding, maintenance and supporter screens remain separate work.

The broader conformance backlog remains open: full Command Center structure, workflow All accounts administration, remaining library/viewer/editor/people/queue flows, complete browser journeys, SDK qualification and release gates. Native/mobile and the dependent Studio editor remain parked as recorded in the original handoff. Hardware and rights checks remain explicit acceptance dependencies. No issue is marked Done merely because a candidate passed review or local tests.

## Hosted qualification after the recovery push

PR #137 was updated to `d8031e062c2d4c5cf8fb216f1815b84c54e9013b` on September 23 at 18:26 UTC. At 18:41 UTC, 28 checks had succeeded; fork integration and both web E2E jobs were still running. This is a dated snapshot, not a claim about the current PR head.

Exact failed-job logs identified these repairs:

- Docs build job `107321950937`: inherited handoff formatting, repaired at `43c267a787`. Full local documentation format and build passed; two existing broken-anchor warnings remain.
- CLI unit job `107321951435`: the migration report test imported the web TypeScript parser through a build path requiring generated SvelteKit state. Commit `4b101d5a5f` loads the same parser through Node's TypeScript transform. All 69 CLI tests, type checking and scoped lint/format passed.
- Server/CLI E2E ARM job `107321951693`: six failures were reproduced and repaired at `db695e188928cf0e2941746c7c54c77a7ebe004b`, integrated at `d7318744e8`. Strict album expectations now include smart-album fields; offline fixtures create isolated roots before library validation. Both complete specs passed 64 tests with one existing skip against the checkout's compiled server. The earlier hosted run recorded 413 server tests passed, six failed and 14 skipped; all 20 CLI E2E checks passed. The x64 sibling was cancelled.

The cookie, Utilities and local hosted-failure repairs described above are newer than the pushed `d8031e062c` checkpoint. Their hosted qualification must be associated with a newer PR head; results from `d8031e062c` do not qualify these changes.

## Additional recovered runtime behavior

The local E2E runtime exposed native-ESM email rendering failures outside the selected HTTP assertions. The failure was reproduced without Docker: both email layouts used a bare CommonJS `require()`. Independently approved commit `c6cf28a4f822e11f2b05409fc03c74549afced72`, integrated at `32dd48944c`, uses `createRequire(import.meta.url)` for the existing preset dependency. Six compiled render checks failed before the change and passed after it, including a production build; 62 focused notification tests passed. The combined integration also passed all six compiled render checks. No external email was sent.

The preserved FL-35 dirty worktree was recovered onto the current integration in approved commit `6cfeb60cfd919ba38da0d2adf16865f353a89190`, merged at `ade8b629a9`. Original playback now bypasses HLS; source changes retire listeners and sessions, and ignore late capability/play completions. Fatal errors offer retry in the prototype viewer message bar. Four baseline regressions failed; seven playback and 17 adjacent viewer tests passed with clean TypeScript, Svelte, lint and formatting. The original dirty worktree is untouched.

A real MP4 uploaded only to the disposable local server decoded and advanced in the browser using both playback and original URLs. A separately uploaded truncated fixture produced the error bar and could retry without changing source mode. Realtime HLS was disabled in that runtime; real HLS decoding, native-HLS behavior and casting remain unqualified. This bounded functional evidence is not complete viewer design acceptance.

FL-71 now owns the next prototype Command Center shell and Overview packet: one rail, the catalog's 19 areas and existing real forms/data. Global scope controls and unavailable metrics must not imply backend capabilities that do not exist. Full control-by-control conformance remains open.

## People recovery and the next hosted checkpoint

FL-37 candidate `cb1988a941338235896d2d78f928797c93d9ea35` was independently approved and integrated at `8af009aad58347e1ac27baf56b4692cc00d84687`. Manage People now follows the September 22 prototype's header, responsive cards, shortcuts, sticky footer and discard dialog. The recovered behavior retains rejected visibility changes for retry, commits only confirmed IDs, deduplicates paginated results and retires pending requests on lock, account change, logout or unmount. Shortcuts explicitly affect loaded people. Fourteen focused checks, web/E2E types, Svelte and scoped lint passed. A browser fixture verified partial-save retry and the phone layout; its API responses are mocked, so it does not establish live-server persistence or privacy enforcement. The original dirty worktree remains preserved.

At `44617d5133627d888e4504482d307ccd045fb5d8`, hosted server unit/lint, PostgreSQL medium tests, both server/CLI E2E platforms, both CLI unit platforms, OpenAPI and SQL checks passed. Both web E2E jobs were still running at 19:18 UTC. Exact failed logs identified four narrow repairs at `0414e15a46`: one obsolete Live Photos navigation expectation after the Utilities relocation, two formatting differences, and a lint false positive for removing an async listener by identity. Fork integration failed on the same obsolete navigation assertion. The hosted web suite recorded 2,473 passed, two skipped and that single failure. Local verification of the corrected integration passed all 30 affected People, playback and iCloud checks; documentation and translation formatting passed. Hosted results from `44617d5133` do not qualify subsequent commits.

FL-83's generic modal cleanup and FL-80's persistent PIN cancellation barrier are still under joint repair. Their shared retry must survive auth-route unmount, wait for in-flight unlocks, confirm the server lock and refresh safe data before exposing content. Neither candidate is accepted as complete yet.

## Existing PR recovery — September 23, 19:43 UTC

The owner's open-PR audit corrected the handoff's blanket superseded assumption. The full comparison is in `frameleaf-open-pr-audit-2026-09-23.md`. PR133 contains PR130/131/132 and, through PR131, PR127. Those implementations and their original regression/browser evidence are recovery sources; the September 22 prototype remains the UI authority.

- Studio recovery `1574d985ad560501a21c166eb09bc4502d11a956` passed independent root review and was integrated at `08706048aa`. All 93 recovered files outside the current README/workflow splice/new receipt match PR133 exactly. Fifteen recovery/conformance/attribution tests, 13 workflow contracts, four profiler tests and 43 headless tests pass. One isolated macOS arm64 build and all 2,646 vendor-file hashes pass. The detailed receipt records artifact identity and limitations. Eight unresolved package notices, 210 blocked resources, zero qualified conformance rows and no production adapter remain explicit. This is recovery of prior work, not new editor implementation or distribution approval.
- Accessibility recovery `69bc9be110110ef3335e2de245a60ca867233134` passed independent review and was integrated at `255e2d0b96`. It restores the existing shared-modal accessible-name patch and native Dialog explicit return-focus capability. Both original regressions failed against the current baseline; 39 affected tests, web TypeScript, scoped lint/format and offline frozen install pass after recovery. The existing WebKit driver passed twelve theme/motion/viewport combinations with zero page errors. Two screenshots were inspected. Explicit return-focus is still opt-in and currently exercised by the fixture; complete production-consumer and screen-reader acceptance are not inferred.
- Combined integration rechecks passed 21 accessibility tests, 15 Studio contract tests and 13 workflow tests. No production engine build was repeated in the integration worktree.
- Confluence primitive page61539380v8 and Studio README page61408007v6 match their complete recovered sources after decoding HTML readback. Existing page61997196v6 matches the recovered rights source; its mapping was restored without rewriting that page. Earlier page bodies and linked resources are preserved. The Markdown page update was rejected for potential rich-content loss; the successful HTML update retained that content.
- At hosted head `fbbdc7970e`, the second snapshot had 31 successful checks, three skips, two web E2E jobs running and one Test Web failure. Its exact job107343269584 log failed only on People-management formatting; `0455268869` applies the formatter's changes. The recovered code has not yet been tested by hosted checks.

The latest local FL83 candidate `b832bb19bd` remains unmerged after independent review found production PIN unlocks bypass its pending-unlock registry. Its shared modal cleanup was accepted, but that does not establish the complete privacy contract. A new isolated browser recovery owns the existing PR131 initial/expiry/remote-revocation guards plus the corrected FL83/FL80 wiring. Server privacy and search/action concurrency recovery are separate owned packets in progress. Original dirty worktrees remain preserved; no default-branch merge, registry publication or deployment occurred.

## Search recovery and historical receipt restoration — September 23, 19:56 UTC

Search/action candidate `0c28c723d0b986b975702ecd26b33c5e33e0683d` passed independent root review and was integrated at `a8e0467379ccd76d900597a5b8180af5f86a9cd3`. It recovers PR133 request identity/abort handling and captured, awaited viewer actions without replacing the current prototype. The owner reproduced 20 failures and three passes against the original integration, then passed 82 focused tests with one existing skip, TypeScript, Svelte and scoped lint/format. The merged integration passed all 29 recovered request/route/action/gallery regression tests plus eight asset utility checks. These are mounted/unit checks with mocked API transport, not authenticated browser qualification.

Commit `8c28e665cc` restores seven missing historical source documents and Confluence identities from PR133. Live readback verified the complete original source on every page; the reviewed-integration page now preserves that source in its version-17 history. No remote page was rewritten by this restoration. All nine missing identities identified by the audit are now represented. The mirror has 82 source receipts: 46 match checked-in bytes and 36 retain preserved historical bytes. A matching source hash does not promote an old test result to current acceptance.

The strict documentation snapshot retains 517 anchors. Changed tracked groups since `8944f8be77` are the plan directory (28 to 34), asset-viewer directory (52 to 53), its actions directory (15 to 16), Frameleaf components (299 to 300), Timeline components (3 to 5), and Frameleaf helpers (207 to 209). Their recovered filenames explain the new hash; all ten documentation mutation checks pass with the updated pin. GitNexus reported no changed symbols for the nine documentation files; the snapshot callback itself is outside its stale index, so the literal-only change was checked directly.

Published PR137 still points to `8944f8be7718d43342acb5c1923329d0e423f1d4`, whose first hosted snapshot had 27 successful checks, three skips and eight running jobs, with no failure. New local commits do not inherit that hosted result. The server privacy candidate remains rejected until authoritative lock transactions, revoked-session validation and deletion notifications are repaired and independently rechecked. Prepared Archive/Undo and browser privacy/auth recovery remain separately owned, unintegrated packets.
