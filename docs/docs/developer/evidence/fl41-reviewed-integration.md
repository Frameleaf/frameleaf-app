# FL-41 reviewed web/server integration

Issue: [FL-41](https://heroit.atlassian.net/browse/FL-41). This is a local qualification assembly, not a replacement for the issue-owned PRs, hosted acceptance, merge, publication or deployment. All mobile work remains deferred under the user's September 21 instruction.

## Source provenance

Assembly runtime commit: `39da25a85f7789909b2400149b574368e311302b`. Default-branch baseline: `a3b0cae7e785e31339353ad9b0a55cfb10d03f56`, including removal of mobile CI. Merge parents preserve the original reviewed commits:

| Issue                                    | Reviewed input                             |
| ---------------------------------------- | ------------------------------------------ |
| FL-29 People filters                     | `79b7f9caa40e4c08fe3c09a75927dcf2984671ea` |
| FL-34 sensitive Locked view              | `aabef632a6f1b1c5390a86a6a4cae265e634778e` |
| FL-39 versions and original-media bounds | `6cd128decbd9ef66f6b43c195ac3bbb582882559` |
| FL-84 reproducible engine build          | `a7882dcf7de1f087a21c93a792c2946ddac4d66d` |
| FL-41 server/SDK validation              | `83100cdaafe199658a51af5ddd3d6f009527720b` |

The 26/39/30/11/6 files changed exclusively by those respective inputs remain byte-identical. Shared changes preserve owner-only sensitive timelines, version APIs, original-media metadata, action-discriminated validation and the stronger no-Dart CI assertions. An independent bounded integration review approved the runtime assembly with no P0/P1/P2 findings, subject to refreshing the tracked-path golden. It did not repeat the individual feature audits.

## Local validation

Before the final original-bounds merge, combined checkpoint `a762277790d71eda5cfd62e84a82dc7301735105` passed 664 server tests, 131 web tests (one existing skip), three SDK transport tests, server/web TypeScript and Svelte checks.

After that merge, the changed paths passed 138 server controller/DTO/asset-service tests, seven video-editor tests, eight version-control tests and three SDK transport tests. Server build, SDK build, web TypeScript and Svelte checks passed; Svelte reported zero errors and warnings. These targeted checks qualify the delta locally; they do not claim a repeated full suite.

Server OpenAPI generation and oazapfts 7.5.0 regeneration are byte-identical to the merged sources:

- OpenAPI SHA-256: `62e8c76f21cc28a79c464cb5528921ba426b6a9bff3ceeab0f75896d04bce3b0`.
- SDK SHA-256: `c5b6359cce7efab60eba52a3fc757660d58db3481b8669061108ae46c13a3f93`.

All 313 existing operations remain, with five additional version operations. Existing operation fields and parameters remain identical except for optional `sensitiveOnly` query parameters on the two timeline endpoints. Existing schemas remain identical except for optional `originalVideo` on `AssetEditsResponseDto`; no existing required fields changed. This structural comparison does not replace hosted OpenAPI compatibility checks.

The final receipt/golden commit `e9e61bbdb97f2605bc2aa3b73b72b9a0886b4aac` passed all 22 documentation/workflow contracts and independent consistency review. Its strict snapshot retains 517 source anchors, with 53 added tracked paths and zero removed paths relative to the FL-41 input; the validator is unchanged.

Starting the installed Docker Desktop resolved the local database blocker. Using the already-cached PostgreSQL and Ryuk images, all 58 PostgreSQL tests passed across the six changed medium-test files: enrichment backfill, migration ledgers, video edit versions, authentication, image enrichment and sensitive timelines. This includes the original-bounds regression. The run used disposable test databases and did not pull registry images or change application databases.

## Remaining qualification

Current-candidate GitHub Actions, broader media qualification and full authenticated application checks remain open. The previously unavailable original-bounds PostgreSQL regression is now locally verified; existing issue-specific limitations beyond that check remain in their receipts. No mobile generator or application validation was reintroduced.

Issue-owned PRs and their monitoring owners remain unchanged. The assembly is unpushed, so its Smart Commit commands have no Jira ingestion receipt. FL-41 remains In Progress. Any merge/publication requires the existing explicit authorization procedure.

## Reviewed follow-ups

The assembly now also incorporates independently approved FL-29 `e6f0c9bc361be93812867ddd5383ad3521ea7fba` and FL-39 `fd9c4b7d39ca7b9ab3b551dc901288aa30808c2e`, combined at `aa47dce6e`. These supersede the corresponding inputs above without rewriting their history.

FL-29 repairs an authenticated-browser keyboard defect: deleting a focused People chip before moving focus let the parent close the dropdown. Focus now moves to its existing People button first. Twelve component tests pass, and the new focus-out regression fails against the previous implementation. Real local Chromium, HTTP authentication, active-fork PostgreSQL and WebSocket notifications verified keyboard selection/removal, owner-only thumbnail access and two-tab lock clearing. A follow-up recovery assertion verifies that both tabs return to the locked application shell and display the empty People picker after the reload. The synthetic fixture and qualification limits are recorded in [the People receipt](../frameleaf-plan/FL-29-people-filters.md).

FL-39 stream-copies qualified pure rotations from original H.264 SDR MP4/MOV inputs into private masters, retaining an independently rendered playback proxy. Inputs with inherited display matrices, incompatible streams or unsupported formats keep their existing baked/fail-closed path. Review found that hardware decoding suppressed metadata autorotation in proxies; the correction disables decoding for rotated masters while retaining the configured encoder. The owner passed 255 media-service tests, including publication-command regressions for four hardware backends, plus 13 media-repository tests. Real FFmpeg tests verify packet/timestamp and decoded-frame preservation. Physical GPU execution remains unqualified; see [the rotation receipt](../frameleaf-plan/FL-39-version-persistence.md).

The combined checkout passed all 22 documentation/workflow contracts. Its server typecheck exposed one new original-bounds test array whose inferred action enum was wider than FL-41's discriminated DTO. Adding the existing `AssetEditActionItem[]` annotation repairs the fixture without changing runtime validation; the server typecheck and all 70 asset-service tests then pass. Runtime source files for both reviewed follow-ups remain byte-identical to their owners' candidates.

## Archive authorization follow-up

A focused active-fork PostgreSQL regression first plans an explicitly Sensitive asset under owner elevation, then submits the remembered archive IDs as the locked owner and as a different elevated administrator. Both archive requests are denied before ZIP creation. The download medium suite passes both tests; server TypeScript and changed-file ESLint pass. This verifies the existing request-time authorization boundary, without changing production code. It does not qualify revocation during an already streaming archive, browser viewer/Trash workflows, or the separately identified direct asset-file privacy gap being repaired by the FL-34 owner.

## Authenticated viewer and Trash follow-up

Using the existing disposable active-fork API and synthetic People-suppression fixture, Chromium loaded a decoded image in the normal photo viewer and then the Trash viewer. Each phase confirmed original download HTTP 200 before a real server session lock. After locking, the asset image disappeared, the Search control and Unlock sensitive content button returned, and fresh preview/original requests returned 400/404. Both phases reported no page errors. The test uses real HTTP authentication and WebSocket notifications without API interception; it adds synthetic preview/thumbnail rows rather than qualifying background derivative generation.

The runnable setup, browser script, assertions, JSON result and before/after accessibility trees are retained in the local authenticated-browser evidence bundle. Backend build `39da25a85f7789909b2400149b574368e311302b` and web `7ebd72e34223f3c24be1660d71d516edc94ea888` retain the reviewed privacy handlers. This closes the named local photo/Trash-viewer lock-flow gap for People-suppressed media; it does not qualify every Sensitive classifier, an already streaming export, direct asset-file endpoints, other browsers or deployed services.

## Reviewed direct asset-file privacy repair

Merge `00f85b4a0d923fad90d5571131a0a248fed94c43` incorporates independently approved FL-34 `02db47e12cdc46430ab4ae6b90450c2106a77395`. Direct asset-file metadata, download and deletion now pass the authenticated privacy context into the existing owner query and apply its hidden-content predicate to the joined source asset. Remembered file IDs no longer bypass source restrictions; owner-only access, legacy Locked elevation and shared-link denial remain enforced. The owner's PostgreSQL regression changes from six failures/four passes to ten passes, with TypeScript and lint passing. The two production files and regression remain byte-identical to that reviewed candidate. See [the derivative receipt](../frameleaf-fl34-derivative-evidence.md) for precise scope and remaining qualification.

## Reviewed reduced-motion picker repair

The assembly incorporates independently approved FL-29 `7b89a77182ccedbc59703dcc07a3b2e503f2ebb0`. The shared Combobox now uses the existing reactive reduced-motion preference to disable its Svelte intro, retaining 250 ms otherwise. The owner's regression fails before the fix and all nine focused tests pass afterward; ESLint and scoped Svelte checks also pass. Chromium observes the normal 250 ms introductions and no listbox animation calls under reduced motion, with ArrowDown/Enter selection working in both cases and no page errors. The merged component and regression remain byte-identical to the reviewed candidate. See [the primitives receipt](../frameleaf-plan/FL-29-web-primitives.md); these local results do not replace hosted or broader accessibility acceptance.

## Reviewed delivery reconciliation after approved geometry merge

Local assembly `bc6fc20e0` preserves the current issue-owned delivery ancestry: FL-29 `fc9d12d38`, FL-34 `f038c4ffc`, FL-39 `3c5c74a95`, FL-41 `82e588e1f` and FL-84 `556b4bf64`. It includes their formatting corrections, current source receipts and shared container-state diagnostics. The diagnostics capture selected Docker state and timestamped logs after API tests, before maintenance; they do not establish the cause of PR131's earlier connection failure or alter production behavior. Source identity comparisons confirm the reviewed files remain unchanged by integration. All 23 combined documentation and workflow contracts pass; inventory conflicts retain the assembly's existing strict digest.

PR129 was merged under explicit authorization at `ad690ddaec4b47733fc84ea8d047849a6d490129`. Its main-branch tests and integration/official-container certification gates passed. At the latest delivery checkpoint, Docker platform builds had passed while remaining manifest/publication jobs were pending. The other issue PRs remain unmerged with fresh hosted checks pending; this local assembly is unpublished. Full image publication, deployment and wider issue acceptance are separate outcomes. All mobile work remains deferred.

## Reviewed combined application candidate — September 21, 2026

Runtime `2693f34c5835b0c848bdad3654ac6465d23aecc8` incorporates the previously reviewed assembly and the subsequent FL-29 shared dialog naming, FL-30 Settings/navigation focus and responsive header, FL-31 search/access lifetime repairs, FL-39 trim coordinates and FL-85 conformance inventory. It retains the user-merged PR134 image-build reduction and the independently reviewed FL-142 immutable image-reuse/provenance safeguards. This runtime received independent integration approval with no P0/P1/P2 findings. Local head `4242313337ec1325ac48a3b5fd4ade537e86a092` adds only the strict source-anchor test receipt; it does not change runtime behavior.

The FL-31 merge preserves both the sensitive-only timeline refresh policy and access retirement: retired timelines reject live upserts before considering a sensitive-only refresh, and Mark Safe still removes its matching asset. The shared access adapter replaces the older duplicate access-change listener. Viewer tests retain both late publication updates and cancelled private refreshes. The owner's successive independent reviews repaired retained detail-panel callbacks, gallery destruction on harmless unlock, and loading state left behind when a pending action's asset changes.

The combined run initially exposed eleven inaccessible-role lookups after lock tests. The actual AuthManager invokes full-document privacy revocation, which conceals the HTML element before navigation. Happy DOM does not replace that document, while ordinary component cleanup only removes body contents. A read-only reproduction established the cause; the shared test setup now removes this display property only after each test. Production concealment, failed-navigation privacy behavior and accessible-role queries are unchanged. The final run passes 148 tests with one pre-existing skip across thirteen search, timeline, viewer, cache and privacy suites. Eleven combined About/overview tests also pass. Whole-web TypeScript and changed-file lint/format pass. These are focused local results, not hosted merge gates or complete browser/privacy acceptance.

The strict documentation suite passes all ten mutation tests after recording source-anchor digest `dfb0b5368700164ec6676b550cabe99c42cb14f878ddcbf520d256fe29b214d8`; its fail-closed validator is unchanged. Existing issue-owned receipts distinguish earlier browser/media qualification from this candidate. Final combined authenticated browser qualification is in progress on the frozen runtime above. The work remains unpublished and unmerged at this checkpoint. No complete Studio conformance, external resource rights, screen-reader/cross-browser, deployment or full-issue acceptance is claimed; mobile/native implementation remains deferred.

## Combined authenticated browser qualification

The bounded combined browser pass succeeded on exact runtime `2693f34c5835b0c848bdad3654ac6465d23aecc8` with fresh frozen dependencies, rebuilt current SDK/server and a disposable synthetic PostgreSQL/Valkey fixture. All eleven Settings overview checks passed, including actual storage/account metrics, history, preferences, utility routes/Back, Trash capability, draft retention and real regular-user admin API 403. Explicitly injected storage/history transport failures recovered through their retry controls. About's accessible name, initial dialog focus, single-Escape dismissal and initiating-trigger restoration passed at 320/390/820/1440 pixels. Header controls remained keyboard reachable without document overflow; settled dark/light and narrow visuals passed. There were no page errors.

Actual session locking produced a new `/photos` document request, cleared an old-document marker and removed elevation. This proves full-document replacement for that tested flow; the browser's navigation timing correctly says `navigate`, not literal reload. All owned services/containers were removed, ports freed and disposable session tokens deleted. The complete runnable receipt and hashes are retained at `/Users/adamtaylor/.codex/worktrees/frameleaf-fl30-combined-evidence/README.md` (SHA-256 `56dbce632de3ffac2b871a12fec63013b3bcd0a94466473e5104acc38d3d3727`). Source remained clean during qualification. Later integration commits through `5c2327525d6455c52ba8ea519c89018514d371aa` only change documentation and its strict expected receipt. These results do not qualify every route, screen-reader/cross-browser behavior, complete issue acceptance, hosted checks or deployed services.

## Published-head CI repair — September 21, 2026

Published candidate `7432c5b7f4c1052a12c6ddd361984b9fb4b95570` failed hosted web semantic checks, documentation formatting and Docker-based E2E/migration builds. These are failures, not qualified delivery evidence.

- Test Web job `106542313882` rejected a derived search value declared before its dependency. FL-31 candidate `2653ed5acdeaa8c9fadc3cb83872d3a35d872ef8` reorders that declaration and correctly types the enrichment test wrapper. Its full Svelte semantic check has zero errors with 76 existing warnings, TypeScript passes, and 29 focused tests pass.
- Web E2E job `106542313831`, server E2E job `106542314293`, and migration job `106542311247` fail at the same web-build import: the approved `frameleaf-logo-white.svg` is excluded from the Docker context and absent from the web stage. The repair admits and copies only this exact source asset, preserving its bytes. It also includes this newly consumed asset in immutable image-reuse input verification; unrelated design changes do not become server build inputs. Docker context extraction matches the original SVG byte-for-byte, and the actual production Vite build passes. The 17 release-contract tests pass.
- Docs job `106542312220` identifies formatting in this file and the FL-30/FL-31 receipts. Formatting is corrected without changing their acceptance claims; source-owned mirrors and hashes are refreshed together.

The image-build reduction from PR #134 remains in place. No required hosted check is removed or weakened, no failed run is represented as green, and new exact-head hosted validation remains required after publication. The Docker-context proof and local production build do not replace the full hosted E2E or migration lanes.

## Reviewed follow-up integration — September 21, 2026

PR133's published `91dad8d2bcd0178f586c0840b6508f6458cf06a7` passed the build, unit, integration, AMD web, both server architectures and all three official/fork migration lanes. ARM web job `106546977629` in run `35664463872` failed repeated permanent deletion while viewing Trash. Downloaded browser traces confirm both DELETE requests returned 204 with correct IDs; a blanket cache invalidation rejected the next photo's pending neighbor lookup. This is an application race, not a missed confirmation. The access-revocation generation guard must remain intact. The failed head is not merge-qualified.

Root integrated independently approved selected-ID Archive `57cec47081a0e811c192ac765f120427fc4e53a0` as `0c29455563712940d18d6fb93ca1f775e3f121b6`. The owner ran actual PostgreSQL and authenticated browser cases for durable worker publication, reload/recent receipt recovery, cancellation/retry, Undo and intervening-edit conflicts. Root combined validation passed thirteen mounted web tests, fifteen archive/server catalog tests and whole-web/server TypeScript after building the workspace SDKs. This does not qualify all-matching Archive or full FN204; those remain follow-on work.

Root integrated independently approved FL86 resource admission `b349ed856d1b92ca839a2db6c1865abbfa585f8d` as `0a325bdc0`. The default-deny policy covers pre-acquisition and warm-use boundaries for models, caches, SDK workers, iframe paths, fonts and Lottie; explicit local media remains usable. Owner editor/headless browser evidence and root five attribution/policy tests pass. All 210 resource decisions remain blocked. Subsequent independently approved notice candidate `42ea135f89c6f3025a2d54e2fbd94cd457f82230` resolves sixteen of the twenty-six missing-notice gaps using exact package/source evidence. The current standalone audit records 184 notice files across 215 packages, with ten explicit blockers. All 210 resource decisions remain blocked and distribution approval remains false. The additional notices have not yet been qualified by a fresh complete engine artifact build. Full rights, complete user/project grants, arbitrary embedded-resource traversal, historical service-worker upgrades and broad Studio acceptance remain open.

The WebKit pointer-focus correction `2895b9b92` was independently approved. The final complete component matrix ran at clean `aacfa8f98d6969e92e840476a152068763a3b015` using Playwright1.62.1 WebKit26.5 and production CSS with explicit full web/src Tailwind scanning. Twelve theme/motion/viewport combinations passed modal inert background, keyboard page focus, Escape/pointer focus restoration, overflow, picker keyboard selection, nested themes and disabled controls; no page errors. The earlier standalone fixture's limited Tailwind scan was corrected before final visual inspection and full rerun. Exact source/harness hashes and diagnostics are retained in `/Users/adamtaylor/.codex/worktrees/frameleaf-fl29-webkit-evidence/README.md`. It is component evidence, not installed Safari, authenticated production or screen-reader qualification.

The owner explicitly confirmed that FL87's administrator-installed CM Analyze/Metafier, automation-rights evidence, owned HDR/Dolby corpus and Dolby/HDR10 QC devices do not exist. VID090 therefore remains unqualified and continues to block broad Studio mounting, full-graph migration and remote-preview integration. This does not block independent library, quick-edit, project review/storage or isolated resource-admission work. No Dolby account, agreement, tool installation or hardware purchase was completed; no metadata-only test substitutes for the required end-to-end picture/PTS and physical-device evidence. All native/mobile work remains deferred.

The independently reviewed FL-31 shared-session foundation and deletion repair `65b4153d477b439db11da66e2110853d34c5b179` is integrated at `8c255be8f828bbea027475c00c193c937b545e26`. Timeline owners retain their scoped manager; generation and access retirement still reject stale work. The real deletion producer now awaits replacement navigation before mutation, and confirmation for an old asset cannot move the newly viewed cursor. Mounted regressions reproduce the actual delete event, asynchronous neighbor lookup and conditional viewer teardown. The same reviewer approved the repair after reproducing the earlier race. Search adaptation, a stable layout host and memory/session coverage remain unfinished.

The separately reviewed action-identity repair `ff66374915a8f3e2681a72faaab34c0e53141771` captures the invoked asset across archive, restore and visibility confirmation; archive failure no longer emits false completion. A follow-up makes Archive and visibility changes await replacement navigation before submitting their mutations, reporting a failed navigation without changing the server. The new mounted checks failed before the repair; the combined seven timeline, viewer, cache, session and action suites pass all 34 tests. Whole-web TypeScript and scoped lint/format pass. These local regressions do not replace new exact-head hosted ARM E2E qualification.

## Compatible archive drain and hosted browser repairs — September 22, 2026

Published `ea4a057e009d6c8cf203ded5c6c7b75f1ae13cf9` passed all three migration lanes, both server E2E architectures, Docs and Studio engine builds. AMD Web E2E finished with 97 passes, four skips and six failures; ARM was cancelled. Four Search Gallery deletion failures arose because the global delete event retired the active cursor before the gallery selected its neighbor. Two archive fixtures still expected an immediate asset PUT although the new durable operation dialog awaited confirmation. Separate failures identified registry import ordering, modal-test teardown timing, the expected catalog table count and the public queue job enum. These terminal results are evidence for that head, not the next candidate.

Reviewed fixture repair `6998f8cfe545f5a00616d0bf4284d412ad2be71b` orders the three server registry imports, awaits actual modal scroll-lock cleanup and updates the exact cutover-lock expectation to include both archive tables. The targeted real PostgreSQL probe and ten About/overview tests pass. The test-only archive repair `a88dfc0cd` exercises fixed selected IDs and a UUID request key, pending and completed receipts, reload, and the original Photos/Albums unarchive assertions; both Chromium journeys pass. Independent review approved the fixture change without production changes or weakened assertions.

The independently approved FL-31 Search adapter `6ce2bc612d2a407472ba2c48c48ca6fd4a7f54fb` preserves metadata, smart and Ask query identity, structured cursors and access retirement. The follow-up Gallery repair `55fdce8e951c2ec0bcc932b3bc985ff403343ba2` awaits guarded DELETE/TRASH neighbor navigation before the global event, and completion removes only the invoked result. Its real-component regression failed before the repair; eight Gallery/Timeline checks and all four original Search browser tests pass. Archive behavior and privacy retirement are retained. Full FN203 and layout/memory scope acceptance remain open.

The compatible FL-32 drain `23c31d81db3d126336984de004640cfbf2a0fc6a` removes the unreleased public job enum and runs existing PostgreSQL receipts through a bounded microservices drain with startup recovery and shutdown handling. Independent review, 39 focused checks and actual stopped-worker/restart/reload/retry/Undo browser qualification pass. Exact combined matching runtime `6a5386acff5d444bf9bc2757f00575ed5642e634` is separately approved and browser-qualified: the server materializes the owned count and membership before confirmation; late uploads remain excluded; unconfirmed preparations remain idle across drain ticks; publication, reload, cancellation/retry and conflict-aware Undo work. All three combined browser page-error arrays are empty, and owned fixtures are cleaned. Migration120 records preparation state; normal queue response contracts match the default branch. This advances the bounded normal-Timeline matching scope, not all scopes or full FN204.

Independently approved FL-86 notice candidate `c43f87ce1d51185cc7fd01d48684d89851fd4227` binds two stable onnxruntime-common notices to exact official source and npm payloads. The current standalone inventory has 186 notice files across 215 packages, with eight unresolved blockers. All 210 resource decisions remain blocked and distribution approval remains false. The earlier ea4a Studio build covers its 184-notice input; a fresh artifact build must qualify the two added notices. Dolby tools, rights evidence, corpus and QC hardware remain unavailable. Mobile/native implementation remains deferred. Fresh exact-head hosted checks, complete issue acceptance, publication and deployment remain distinct pending outcomes.

## Reviewed Photos and Albums layouts in the combined candidate

FL-33 candidate `e5de571e8c753bcfdb0c9337ecd078fce8ed0266` received independent approval and is integrated at `4fc7323e6`. The existing preview preference enables Timeline, Browse and Work on real Photos and Album detail while retaining the paginated session, selection, anchor and mounted viewer/editor. Its exact runtime passed authenticated Chromium checks for two-month pagination, responsive layout, real video playhead, image/video drafts and deletion/session-lock retirement. The owner passed 69 focused checks and the reviewer independently passed eight. Search/Memory/Spaces layouts, production-scale and exhaustive accessibility/privacy/browser acceptance remain open.

The resulting combined source passes all 28 layout, Photos, archive modal and real Gallery/Timeline deletion tests across seven suites. Whole-web TypeScript and Svelte checks pass with zero errors or warnings. The additive matching Archive action remains present on Photos; no conflict resolution dropped it. This local combined qualification does not replace the pending exact-head hosted run or establish any issue as Done.
